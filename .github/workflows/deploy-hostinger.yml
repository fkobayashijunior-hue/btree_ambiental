name: Deploy para Hostinger (LEGADO - DESATIVADO)

# Deploy automatico desativado: o site agora roda no VPS (ver deploy-vps.yml).
# Mantido apenas como referencia / disparo manual, se necessario.
on:
  workflow_dispatch:

jobs:
  noop:
    name: Desativado
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy para o compartilhado Hostinger foi desativado. O site roda no VPS. Use o workflow 'Deploy para VPS'."
